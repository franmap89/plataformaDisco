function toggleFavorite(element) {
    element.classList.toggle('text-gray-400'); // ejemplo de cambio de estilo
    element.classList.toggle('text-yellow-500'); // cambio a estado favorito
    // Aquí puedes agregar lógica adicional para actualizar el estado en el backend si es necesario
}